[project]
name = "esp32c6-sniffer"
version = "0.1.0"
description = "Passive tri-radio wireless sniffer on an ESP32-C6, streaming into Wireshark"
requires-python = ">=3.12"
license = "MIT"
license-files = ["../LICENSE"]
authors = [{ name = "Mathew Newman" }]
keywords = ["wireshark", "extcap", "802.15.4", "zigbee", "thread", "wifi",
            "bluetooth", "ble", "esp32", "esp32c6", "sniffer", "pcapng"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Operating System :: Microsoft :: Windows",
    "Programming Language :: Python :: 3",
    # Tested on 3.14t in CI; cross-thread state goes through locks. Not
    # audited beyond that, hence Beta rather than Stable.
    "Programming Language :: Python :: Free Threading :: 2 - Beta",
    "Topic :: System :: Networking :: Monitoring",
]
dependencies = ["pyserial>=3.5"]

[project.optional-dependencies]
# build is what extcap/package-release.py shells out to for the wheel that
# ships in a release. Declared rather than assumed, so the release path
# works from a fresh clone and in CI without a separate install step.
dev = ["pytest>=8.0", "build>=1.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
